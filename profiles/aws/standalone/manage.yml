parents:
  - aws/config

profile:
  base-network:
    module: cluster
    profile: aws/network
    operations: run

  zimagi-network:
    requires: base-network
    profile: aws/network

  zimagi-admin:
    requires: zimagi-network
    profile: aws/server
    deploy_zimagi: false
