parents:
  - aws/config

config:
  admin_server_host_name: default

profile:
  base-network:
    module: cluster
    profile: aws/network
    operations: run

  zimagi-network:
    requires: base-network
    profile: aws/network

  zimagi-admin:
    requires: zimagi-network
    profile: aws/server
    deploy_zimagi: true

post_run:
  zimagi-server-rotate:
    command: user rotate

post_destroy:
  zimagi-admin-host:
    command: host remove
